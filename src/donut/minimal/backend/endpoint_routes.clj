(ns donut.minimal.backend.endpoint-routes
  (:require
   ;; NOTE this is only here because :require can't be empty and we
   ;; want :require for generators to work. Feel free to remove
   clojure.core))

(def routes
  [;; example
   ])
